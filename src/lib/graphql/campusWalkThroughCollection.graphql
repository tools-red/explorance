query walkthroughScriptsCollection {
  walkthroughScriptsCollection {
    items {
      videoFile
      sequenceNumber
      aiAvatarVideo
      videoDataType
      videoCaptions {
        json
      }
    }
  }
}
